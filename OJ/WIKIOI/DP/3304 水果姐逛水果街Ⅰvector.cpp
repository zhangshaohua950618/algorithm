#include<iostream>
#include<vector>
#define MAX(a , b) ((a) > (b) ? (a) : (b))
#define MIN(a , b) ((a) < (b) ? (a) : (b))
using namespace std ;
const int MAXN = 200000 + 10 ; 
typedef vector<int> v ;
v dp[MAXN] ;
int p[MAXN] , N , x , y , m ;
int main(){
	cin >> N ;
	for(int i = 0 ; i < N ; i++)
	cin >> p[i] ;
	cin >> m ;
	for(int i = 0 ; i < m ; i++){
		cin >> x >> y ;
		
	}
	
} 
