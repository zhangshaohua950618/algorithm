#include<iostream>
using namespace std;
const int MAXN = 10001;
int a , b , c;
int x[4] , y[4] , z[4];
bool ok(){
	
}
int main(){
	int i , j;
	cin >> a >> b >> c;
	for(i = 1 ; i <= 3 ; i++){
		cin >> x[i] >> y[i] >> z[i];
	}
	for(X = 1 ; X < MAXN ; X++)
	for(Y = 1 ; Y < MAXN ; Y++)
	for(Z = 1 ; Z < MAXN ; Z++){
		if(X * x[1] + Y * x[2] + Z * x[3] % a == 0 ){
			cout << 
		}
	}
	cout << "NONE" << endl;
	return 0;
	
}
